scripts/relay-work/
*.bak-*

# Plugin SDK symlink (created by install.sh)
scripts/openclaw-newcc-plugin/node_modules/

# OpenClaw instance persona/state (not part of the distributable skill)
/AGENTS.md
/SOUL.md
/IDENTITY.md
/TOOLS.md
/USER.md
/HEARTBEAT.md
/.openclaw/

# Alt-model settings may carry a provider API key: keep local copies out of git.
# The repo ships only the REPLACE_WITH_* template committed explicitly.
relay-work/
